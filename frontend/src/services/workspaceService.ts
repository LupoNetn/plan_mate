import api from "../utils/axios";

interface CreateWorkspaceParams {
    name: string;
    description: string;
}

export const fetchWorkspaces = async () => {}

export const createWorkspace = async ({name,description}: CreateWorkspaceParams) => {}